<view class="quick-action">
  <view
    class="action-item"
    wx:for="{{actions}}"
    wx:key="id"
    bindtap="onAction"
    data-id="{{item.id}}"
  >
    <text class="action-icon">{{item.icon}}</text>
    <text class="action-text">{{item.text}}</text>
  </view>
</view>
